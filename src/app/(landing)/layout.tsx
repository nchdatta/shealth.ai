const LandingLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      {/* Main content area */}
      <main>{children}</main>
    </>
  );
};

export default LandingLayout;
